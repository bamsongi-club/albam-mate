# ADR-0012: API 요청 경계에서도 방의 시간 기반 상태를 보정

- 상태: 승인됨
- 작성일: 2026-07-24
- 결정일: 2026-07-24
- 관련: [ADR-0004](0004-room-state-transition-scheduler.md), [ADR-0005](../participation/0005-room-participation-optimistic-locking.md), [ADR-0009](../platform/0009-utc-time-standard.md), [트랜잭션 컨벤션](../../CONVENTIONS.md#transaction), [P0 방 상태 계약](../../P0-spec.md#방-상태roomstatus), [API 방 상태 계약](../../API.md#roomstatus)
- 대체 대상: ADR-0004
- 후속 ADR: 없음

## 맥락

ADR-0004는 Java·Spring 내장 스케줄러가 방의 시간 기반 상태를 주기적으로 갱신하도록 결정하고, API 요청 시 상태를 보정하는 방식은 제외했다.

하지만 P0 계약은 `now >= startsAt`이면 방이 `CLOSED`라고 정의한다. 스케줄러 실행 전까지 저장 상태가 `RECRUITING`으로 남으면 API가 `status = RECRUITING`과 `joinable = false`를 함께 반환하거나, 시작 시각이 지났는데도 주최자 종료 요청을 거절할 수 있다.

스케줄러 주기를 줄여도 이 불일치 구간은 사라지지 않으며, 정확한 시각 실행을 위한 별도 인프라 도입은 P0 범위를 넘는다. 따라서 API 요청 경계에서 현재 시각에 맞게 상태를 보정하고, 요청이 없는 방은 기존 스케줄러가 정리하는 방식을 결정할 필요가 있다.

## 검토한 대안

| 대안 | 장점 | 비용·위험 | 판단 |
| --- | --- | --- | --- |
| 스케줄러만 사용하고 상태 반영 지연을 API 계약으로 허용 | 요청 경로에서 추가 쓰기와 충돌 처리가 필요 없다. | API가 일정 시간 이전 상태를 반환하고, 스케줄러 지연의 상한을 보장하기 어렵다. | 제외 |
| API 요청 경계에서 상태를 조건부 보정하고 스케줄러를 함께 사용 | 요청 시 정확한 상태를 보장하면서 요청이 없는 방도 정리할 수 있다. | 조회도 상태 변경을 일으킬 수 있고, 동시 요청의 충돌과 재시도를 처리해야 한다. | 선택 |
| 정확한 시각 실행을 보장하는 외부 스케줄링 시스템 도입 | 요청 여부와 관계없이 기준 시각에 가까운 상태 전환을 중앙에서 관리할 수 있다. | 현재 P0보다 구축·운영 범위가 크고 요청과 작업의 동시 실행 문제는 별도로 처리해야 한다. | 제외 |

## 결정

방 목록·상세·내 모임 조회와 방 상태에 의존하는 수정·취소·종료·참가·참가 취소 명령은 애플리케이션 서비스 진입점에서 ADR-0009의 `Clock`으로 요청 전체에 사용할 하나의 기준 시각을 얻고, 원래 로직을 처리하기 전에 저장된 방 상태를 조건부로 보정한다.

- `status = RECRUITING && now >= startsAt`이면 `CLOSED`로 변경한다.
- `status = CLOSED && now >= startsAt + 24시간`이면 `FINISHED`로 변경한다.
- 두 기준 시각이 모두 지난 이전 상태는 위 전이를 순서대로 적용해 최종 상태까지 보정한다.

상태 보정은 기대한 이전 상태를 조건으로 수행하는 멱등 갱신으로 구현한다. `CANCELED`, `FINISHED` 또는 동시에 다른 요청이 변경한 상태를 덮어쓰지 않으며, 성공한 보정은 `ROOMS.version`을 증가시킨다. 충돌과 재시도는 ADR-0005의 계약을 따른다. 조회 요청을 포함해 최초 시도와 재시도 2회가 모두 방 버전 충돌로 실패하면 API는 `409 ROOM_CONCURRENT_MODIFICATION`을 반환하고, 클라이언트는 조회 요청 전체를 다시 시도한다.

조회 요청의 상태 보정은 저장 상태를 변경하지 않는 조회에 `readOnly = true`를 사용하는 컨벤션의 명시적 예외다. 트랜잭션 없는 재시도 조정자가 별도 상태 보정 실행 서비스를 호출하고, 실행 서비스는 각 시도마다 독립된 쓰기 트랜잭션에서 최신 상태를 다시 읽어 조건부 갱신한다. 성공한 보정이 커밋된 뒤 읽기 전용 조회 서비스가 최신 상태를 읽어 응답하며, 읽기 전용 트랜잭션 안의 더티 체킹으로 보정을 시도하지 않는다. 방 목록과 내 모임 목록은 보정이 커밋된 뒤 상태 필터와 페이지를 계산한다.

명령 요청은 상태 보정과 원래 업무 규칙 평가·변경을 같은 시도의 독립된 쓰기 트랜잭션에서 처리한다. 스케줄러도 별도의 전이 규칙을 두지 않고 같은 상태 보정 규칙을 사용해 요청이 없는 방을 주기적으로 정리한다.

## 결과

- 얻는 것: 스케줄러 실행 시점과 관계없이 API가 현재 시각에 맞는 방 상태를 사용하며, 조회와 명령이 같은 전이 규칙을 공유한다.
- 감수할 비용·위험: 조회가 데이터베이스 쓰기와 버전 충돌을 일으킬 수 있고, 목록 조회 전 일괄 보정의 범위와 성능을 관리해야 한다.
- 후속 작업: 공통 상태 보정 서비스와 조건부 갱신 쿼리를 구현한다. 목록·상세·내 모임 조회에서 보정이 커밋되고 보정 뒤 상태로 필터·페이지·응답을 계산하는지, 두 시간 경계와 조회·명령 충돌이 최대 3개 독립 트랜잭션 뒤 `ROOM_CONCURRENT_MODIFICATION`으로 귀결되는지를 테스트한다.

## 보류 및 재검토

- 지금 하지 않는 것: 외부 스케줄링 시스템 도입, 다중 인스턴스 스케줄러 실행 조정 방식 확정
- 보류 이유: P0에서는 요청 경계의 조건부 보정과 내장 스케줄러로 현재 상태 계약을 충족할 수 있다.
- 다시 검토할 조건: 목록 조회의 상태 보정 쓰기가 병목이 되거나, 다중 인스턴스에서 스케줄러 중복 실행이 운영 문제로 확인되거나, 상태 전환 시각에 별도 SLA가 필요해질 때

## 참고 자료

- 이 문서의 맥락·대안으로 갈음

## 검증

- 상태: 검증됨
- 근거:
    - 구현:
        - `room.statuscorrection`의 `RoomStatusCorrectionCoordinator`와 `RoomStatusCorrectionExecutor`는 `room.service.RoomOptimisticLockRetrier`로 낙관 락 충돌만 최대 세 개의 독립 쓰기 트랜잭션에서 재시도하고, 소진 시 `ROOM_CONCURRENT_MODIFICATION`을 반환한다.
        - 목록·내 모임 조회는 필터와 페이지 계산 전에 due 방을 보정하며 `RoomStatusCorrectionScheduler`도 같은 규칙을 재사용한다.
    - 테스트:
        - `room.entity.RoomStatusCorrectionTest`는 두 시간 경계와 연속 전이, `CANCELED`·`FINISHED` 상태 보존을 확인한다.
        - `room.statuscorrection.RoomStatusCorrectionExecutorTest`는 버전 증가·멱등성과 외부 트랜잭션 롤백에도 `REQUIRES_NEW` 보정이 커밋됨을 확인한다.
        - `room.statuscorrection.RoomStatusCorrectionCoordinatorTest`는 재시도 상한과 충돌 오류 변환을 확인한다.

> 상태 값과 번호·대체 규칙은 [루트 README](../README.md)를 따른다.
