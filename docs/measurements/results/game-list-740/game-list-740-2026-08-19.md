# GAME 목록 #740 최신 develop 재측정 — 2026-08-19

## 결론

- PR #771은 원격 `develop`의 `69e595513378d9e0569f5b35221a7441f194f62e`을 반영한 뒤 `044dd1bad7bfcfb3de45245233887a159691bcfb`에서 다시 측정했습니다.
- 세 차례의 별도 runner 실행 모두 6개 시나리오의 20/20 HTTP `200`, 요청 `page=0`, `size=24`의 실제 페이지 메타데이터 계약, 그리고 검증된 `app1`/`app2` upstream 역할을 통과했습니다.
- 단일 batch를 canonical p95로 선정하지 않습니다. relation/complex의 p95가 세 batch에서 크게 달라 raw JSON 3개와 범위를 함께 보존합니다.
- 최신 기본 요청 capture는 proxy HTTP window, `X-Albam-Mate-Upstream: app2`, 해당 Spring container log, 같은 PostgreSQL backend PID `76`의 content/count/related SQL을 함께 보존합니다. SQL execute 합은 `16.981ms`이고 count `16.373ms`가 가장 큽니다.

## 실행 provenance

| 항목 | 실제 값 |
| --- | --- |
| 기준 `origin/develop` | `69e595513378d9e0569f5b35221a7441f194f62e` |
| serverCommit / runnerCommit | `044dd1bad7bfcfb3de45245233887a159691bcfb` |
| runnerFileSha256 | `1cdf579a0cc482bbefc78ae9ec4491d4003d4272b944278e1b313501910ebe75` |
| runnerSourceClean | `true` |
| server runtime | `app1`/`spring-1`, `app2`/`spring-2`의 OCI revision label이 serverCommit과 일치; 공통 image ID `sha256:dbad71e867ca17bd30f13a552a5ba94e24357e3dddaf8673e8339a79a28a9a1a` |
| dataset | games `170005`, v4 ZIP SHA-256 `d4abcf8ff91c0551ac6bc9afdb87ccae007ce46ad8139689ccb01a5c92c537c8` |
| metadata rows | mechanism `428488`, theme `461973`, category `17337`, player preference `263463` |
| warm-up / measured | scenario마다 `5 / 20` |
| percentile | nearest-rank `ceil(p * N)` |

## 반복 실행 조건과 batch 해석

- `albam-mate-771` 전용 Compose의 PostgreSQL·Redis·Spring 2개 인스턴스·proxy를 동일 image로 한 번 기동한 뒤 batch 1~3을 순차 실행했습니다. 세 batch 사이에는 서버를 재시작하지 않았습니다.
- DB/OS cache를 비우지 않았으므로 이는 warm-cache 측정입니다. runner는 순차 요청만 보냈고 별도 부하를 의도적으로 추가하지 않았습니다. Compose health check는 `/actuator/health`를 사용하므로 game-list content/count 요청을 만들지 않습니다.
- runner는 시작과 종료에 두 Spring container의 OCI revision label, 동일 image ID, Compose service label을 대조하고, discovery와 모든 실측 응답의 `X-Albam-Mate-Upstream`이 그 검증 집합에 속하지 않으면 성공 artifact를 만들지 않습니다.
- 안전성 보강 전 2026-08-18 artifact는 응답·provenance guard 이전 결과라 최신 완료 근거에서 제외합니다. 이번 세 batch의 p95 편차도 한 개의 빠른 결과를 대표값으로 과장하지 않는 이유입니다.

## HTTP baseline

| scenario | batch 1 p50 / p95 (ms) | batch 2 p50 / p95 (ms) | batch 3 p50 / p95 (ms) |
| --- | ---: | ---: | ---: |
| `base` | 20.736 / 23.547 | 19.213 / 23.184 | 19.101 / 28.474 |
| `keyword` | 7.623 / 10.915 | 6.827 / 10.450 | 6.313 / 9.884 |
| `player-count` | 48.087 / 50.792 | 58.347 / 69.302 | 62.561 / 86.444 |
| `relation-theme-mechanism` | 224.128 / 257.831 | 202.302 / 223.263 | 214.075 / 239.818 |
| `complex` | 208.714 / 224.044 | 194.545 / 220.303 | 217.350 / 241.847 |
| `flags-upcoming-exact` | 11.480 / 16.459 | 7.817 / 10.279 | 7.004 / 10.894 |

각 batch의 raw sample과 실제 page metadata는 다음 JSON/CSV에 보존했습니다.

- batch 1: [JSON](./game-list-740-2026-08-19T02-30-52.470Z.json), [CSV](./game-list-740-2026-08-19T02-30-52.470Z.csv)
- batch 2: [JSON](./game-list-740-2026-08-19T02-33-53.128Z.json), [CSV](./game-list-740-2026-08-19T02-33-53.128Z.csv)
- batch 3: [JSON](./game-list-740-2026-08-19T02-34-06.860Z.json), [CSV](./game-list-740-2026-08-19T02-34-06.860Z.csv)

세 JSON의 scenario별 page metadata는 일관됩니다.

| scenario | totalElements | content length | totalPages | hasNext |
| --- | ---: | ---: | ---: | --- |
| `base` | 170005 | 24 | 7084 | true |
| `keyword` | 1 | 1 | 1 | false |
| `player-count` | 145310 | 24 | 6055 | true |
| `relation-theme-mechanism` | 70 | 24 | 3 | true |
| `complex` | 38 | 24 | 2 | true |
| `flags-upcoming-exact` | 0 | 0 | 0 | false |

## 기본 요청 controller / SQL / HTTP capture

기본 요청을 proxy로 1회 실행해 HTTP 시작/종료 시각, 응답의 upstream 역할, 해당 Spring container controller log, PostgreSQL raw execute log를 같은 67ms window로 대응했습니다. PostgreSQL `log_line_prefix=%m [%p]`의 backend PID `76`에서 content/count/related 세 statement가 순서대로 실행됐고, health check는 game-list URL을 호출하지 않습니다. [raw capture](./game-list-740-2026-08-19-base-request-capture.log)와 기존 [EXPLAIN 원문](./game-list-740-2026-08-19-base-explain.txt)을 보존했습니다.

| layer | duration | evidence |
| --- | ---: | --- |
| HTTP total | 32.379ms | proxy request, status `200`, window `02:35:00.021Z`–`02:35:00.088Z` |
| controller | duration 미기록 | 같은 `app2` container의 `game search completed` log at `02:35:00.063Z` |
| game content SQL | 0.527ms | PostgreSQL execute PID `76`, `offset=0`, `fetch=24` |
| game count SQL | 16.373ms | PostgreSQL execute PID `76` |
| related room SQL | 0.081ms | PostgreSQL execute PID `76` |
| SQL execute sum | 16.981ms | 같은 PID의 세 statement 합계 |

기존 EXPLAIN은 base content가 `ix_games_popularity_score_name_id` index scan으로 `0.621ms`, base count가 `games_pkey` parallel index-only scan으로 `14.376ms`임을 보입니다. 이번 capture에서도 count가 기본 요청 DB statement 1순위이지만, controller duration을 현재 console profile이 내보내지 않으므로 HTTP 잔여 구간은 수치로 단정하지 않습니다.

## 범위 판정

이번 PR은 최신 `develop`에서의 측정·진단 evidence와 runner 계약만 보강합니다. batch 편차의 원인을 통제하는 load-isolation·cold-cache 실험, residual 세부 계측, count 또는 relation query 개선은 별도 승인 범위에서 다룹니다.
