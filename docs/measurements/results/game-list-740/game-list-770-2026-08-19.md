# GAME 목록 #770 Slice 실측 — 2026-08-19

## 결론

- `bdc23f4901f80c0d0bcbbb6b6430f90dabc73cae` 서버·runner를 같은 commit으로 기동하고, `170,005`건 fixture에서 6개 시나리오를 세 번씩 실행했다. 모든 batch는 scenario마다 warm-up 5회·실측 20회와 HTTP `200` 20/20을 통과했다.
- 모든 실측 응답은 `content`, `page`, `size`, `hasNext`만 포함했고 `totalElements`·`totalPages`는 한 번도 나타나지 않았다. runner는 시작·종료 모두 Compose PostgreSQL의 `games=170005`을 직접 대조했다.
- 단일 기본 요청 raw capture의 app1 PostgreSQL PID `66`에는 `fetch first 25` content statement와 24개 ID의 room aggregate만 있다. 게임 목록 exact total의 `select count(g1_0.id)` statement는 없다. room aggregate의 `count(r1_0.id)`는 기존 조립 경로라 별도로 남는다. [원문 capture](./game-list-770-2026-08-19-base-request-capture.log)를 보존한다.
- 숫자 지연은 서로 다른 기동 시점의 독립 batch라 인과적인 개선 폭으로 단정하지 않는다. 특히 after batch 1에는 재기동 직후 tail이 섞였으므로, 세 원문을 모두 보존하고 빠른 batch 하나만 대표값으로 고르지 않는다.

## 실행 provenance

| 항목 | Before Page | After Slice |
| --- | --- | --- |
| server / runner commit | `ccc6971da796131337e24fa2dd53303e1dcd46f4` | `bdc23f4901f80c0d0bcbbb6b6430f90dabc73cae` |
| runner source clean | `true` | `true` |
| fixture | games `170005`, v4 ZIP SHA-256 `d4abcf8ff91c0551ac6bc9afdb87ccae007ce46ad8139689ccb01a5c92c537c8` | 동일 |
| 측정 | scenario마다 warm-up 5회, 실측 20회, 순차 요청 | 동일 |
| percentile | nearest-rank `ceil(p * N)` | 동일 |

After runtime은 `app1`/`app2`의 OCI revision label과 공통 image ID `sha256:72b4981b9ee66584ac0e20eaab578600a0d3f21b21451dfd5907a0382901a5c1`가 serverCommit에 일치하고, proxy/upstream role·network address를 runner가 시작·종료에 대조했다.

## Before / After HTTP 범위

각 칸은 세 독립 batch의 `p50 / p95 / max` 최소–최대 범위(ms)다. 원문 JSON이 각 표본·정확한 개별 값을 보존한다.

| scenario | Before Page | After Slice |
| --- | ---: | ---: |
| `base` | 18.258–23.453 / 24.488–35.329 / 24.952–54.527 | 6.474–25.057 / 9.171–75.152 / 10.268–111.026 |
| `keyword` | 6.281–9.638 / 9.041–18.025 / 10.808–19.862 | 6.502–37.414 / 8.814–277.600 / 9.965–405.177 |
| `player-count` | 48.564–59.456 / 72.868–76.050 / 73.666–78.993 | 6.061–28.545 / 8.024–99.027 / 8.988–267.046 |
| `relation-theme-mechanism` | 190.300–197.162 / 208.009–211.326 / 215.767–219.702 | 171.191–267.113 / 194.589–435.843 / 229.688–477.433 |
| `complex` | 188.168–190.868 / 205.155–219.857 / 210.252–231.712 | 164.459–287.606 / 216.264–894.512 / 216.593–978.581 |
| `flags-upcoming-exact` | 7.057–10.079 / 11.500–14.951 / 12.847–45.261 | 10.651–12.374 / 16.083–22.245 / 18.245–22.948 |

Before Page 원문은 [#740 최신 baseline 기록](./game-list-740-2026-08-19.md)에 연결된 세 JSON/CSV다. After Slice 원문은 다음과 같다.

- batch 1: [JSON](./game-list-740-2026-08-19T06-36-42.525Z.json), [CSV](./game-list-740-2026-08-19T06-36-42.525Z.csv)
- batch 2: [JSON](./game-list-740-2026-08-19T06-37-05.122Z.json), [CSV](./game-list-740-2026-08-19T06-37-05.122Z.csv)
- batch 3: [JSON](./game-list-740-2026-08-19T06-37-28.810Z.json), [CSV](./game-list-740-2026-08-19T06-37-28.810Z.csv)

## 기본 요청 HTTP / controller / SQL capture

raw capture만 PostgreSQL `log_min_duration_statement=0`을 켜서 실행했고, 위 HTTP batch는 기존 baseline과 같은 일반 로그 조건에서 실행했다.

| layer | 실제 값 | evidence |
| --- | ---: | --- |
| HTTP total | 71.241ms | `GET /api/games?upcomingOnly=false&playerCountExact=false&page=0&size=24`, status `200` |
| controller | `2026-08-19T06:35:06.546Z` event | app1 `game search completed`; local console format은 key-value `durationMs`를 출력하지 않음 |
| Slice content SQL | 1.154ms | app1 PID `66`, `offset 0`, `fetch first 25` |
| related room SQL | 0.157ms | 같은 PID의 24 game ID aggregate |
| 게임 목록 exact total SQL | 없음 | 같은 read-only transaction에 `select count(g1_0.id)` 없음; room aggregate `count(r1_0.id)`는 유지 |

proxy measurement ID는 `game-list-770-base-bdc23f4`이고 응답 upstream `app1` / `172.18.0.5:8080`는 Compose inspect address와 일치한다. direct fixture count는 runner가 `PGAPPNAME=game-list-baseline-fixture-check`로 별도 확인하므로 app SQL capture에 섞이지 않는다.
